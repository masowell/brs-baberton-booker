services:
  - type: web
    name: brs-baberton-booker
    env: python
    buildCommand: pip install -r requirements.txt && playwright install chromium --with-deps
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT